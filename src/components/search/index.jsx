import Input from "@mui/material/Input";

export const Search = () => {
  return (
    <Input
      placeholder="Search"
      sx={{
        color: "black",
        padding: 0.5,
        background: "white",
        borderRadius: 2,
      }}
    />
  );
};
